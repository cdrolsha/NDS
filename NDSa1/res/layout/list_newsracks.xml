<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical" >
    
    <!-- newsrack id / Hidden by default -->
    <TextView
        android:id="@+id/newsrack_id"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:visibility="gone" />
    
    <!-- newsrack name  -->
    <TextView
        android:id="@+id/newsrack_name"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:textColor="#890500"
        android:textStyle="bold" 
        android:paddingTop="5dip"
        android:paddingBottom="3dip"
        android:paddingLeft="10dip" />
    
    <!-- newsrack description -->
    <TextView 
        android:id="@+id/newsrack_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/newsrack_name" 
        android:textSize="14sp"
        android:textStyle="italic"
        android:textColor="#120a23"
        android:paddingTop="2dip"
        android:paddingBottom="5dip"
        android:paddingLeft="15dip" />  

</RelativeLayout>